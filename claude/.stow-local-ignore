# Ignore everything in .claude directory except CLAUDE.md, hooks directory, commands directory, and settings.json
# This will ignore all files that are NOT CLAUDE.md, hooks/, commands/, or settings.json
^/\.claude/(?!(CLAUDE\.md$|hooks(/.*)?$|commands(/.*)?$|settings\.json$)).*