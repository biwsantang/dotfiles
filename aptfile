#!/usr/bin/env aptfile

# trigger an apt-get update
update

# install a ppa
ppa "neovim-ppa/unstable"

# install some packages
package "zsh"
package "fish"
package "stow"
package "git"
package "jq"
package "neovim"
package "fzf"
package "eza"
package "tmux"
package "bat"
package "ripgrep"
package "curl"
package "wget"

# Note: zellij needs manual installation on Ubuntu
# zellij: installed via cargo or download binary